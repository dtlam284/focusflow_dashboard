# Base CMS

This is a code bundle for Base CMS.

## Running the code

Copy `.env.example` to `.env` and adjust API URL if needed.

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
